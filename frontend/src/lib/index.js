// lib/ is reserved for thin wrappers around third-party libraries.
// Example usage: wrap Chart.js here so the rest of the app never imports chart.js directly.
// Currently empty — chart setup lives in utils/chartSetup.js.
// Re-export it here once the migration to lib/ is complete.
