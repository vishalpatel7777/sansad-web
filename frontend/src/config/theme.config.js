// Design token reference — mirrors tailwind.config.js values.
// Import these in JS contexts (chart colors, inline styles) so tokens stay in sync.

export const COLORS = {
  primary: "#1d4cd7",
  sovereignBlue: "#1d4cd7",
  backgroundLight: "#f6f6f8",
  backgroundDark: "#111521",
  inkGray: "#0e111b",
};

export const FONTS = {
  display: ["Public Sans", "sans-serif"],
};

export const BORDER_RADIUS = {
  DEFAULT: "0.25rem",
  lg: "0.5rem",
  xl: "0.75rem",
  full: "9999px",
};
